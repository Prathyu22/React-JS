We cannot have several elements so we can nest elements in React.
They cannot be multiple parents.
In React, no "class" keyword is used.

forEach() ----> Donot return anything.
          ----> Therefore we use "map()"

*** sfc ---> shortcut to create function component.