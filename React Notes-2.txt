React component should always return something.
React can always keep track of changes on Props/states but not on variables.

UNMOUNTED ---> If we shift from one component to other, the first clicked component gets unmounted.