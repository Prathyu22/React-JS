jsx ----> b/w JS and HTML.
Browser doesn't understand jsx.
It understands only the language of React.createElement...

TRANSPILATION : Converting of one lang to other...
                (JSX to React)
                Transpilation tools does this.

WHY JSX ?
React.createElement makes code complex, hence we need jsx.

command for react app creation ---> npx create-react-app myapp